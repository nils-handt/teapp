# Android and web visual parity — canonical mismatch ledger

This is the single tracked mismatch ledger for the active `plan-v2.md`. The production web reference remains the expected output. Measurements are CSS pixels unless stated otherwise.

## Capture context

| Property | Production web | Android API 36 | Classification |
| --- | --- | --- | --- |
| Source revision used by current tracked references | `155fda8497895554808c0bacc6b6ccf12128275d` | Same | Matching committed source; historical `baselines/unfixed` remains unchanged |
| Fixture | seed `visual-parity-v1`, time `2026-08-11T12:00:00.000Z` | Same, restored through Settings | Matching |
| History states | `/tabs/history`, collapsed or expanded filters as named, scroll 0 | Same production route/state, scroll 0 | Matching |
| Layout viewport | 411×683, DPR 1 | 411×683, DPR 2.625 | Matching CSS viewport |
| Visual viewport | 411×683 | 411.429×683.429 | DPR/crop quantization; the 1080×1794 app crop is not an integer multiple of DPR |
| Safe area | 0px on all sides | 0px on all sides | Matching |
| Ionic mode | document and components use `md` | document and components use `md` | Matching |
| Font readiness | Roboto 400/500 loaded; 300 unused and unloaded | Same | Matching |

## `history-filters`

Status: material layout and styling drift resolved. The remaining 1.638% changed pixels are classified as DPR/rasterization after computed-style equality and subpixel geometry bounds were established. This does not complete Phase 2 for the other canonical states.

| Element or region | Web measurement | Android measurement before fix | Difference and category | Evidence and winning declaration | Shared fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Searchbar host | rect `(25,21) 261×44`; padding 0; `--background:#ffffffad`; radius 16px; shadow none; color `#243126`; placeholder `#68756a` at opacity 0.6 | rect `(24.762,20.762) 261.905×58`; padding 8px; background `#fff`; radius 2px; Material elevation shadow; color `#262626`; placeholder initial | Real Ionic cascade drift; later equal-specificity Material rule added 14px to the row | Teapp `.zen-list-search` lost to later `.sc-ion-searchbar-md-h`. Matched rules identify 8px padding, 2px radius, elevation, and Ionic palette as the winners. | Raise the shared selector to `ion-searchbar.zen-list-search`; no platform fork | Resolved: Android rect `(24.762,20.762) 261.905×44`, padding 0, and all listed variables match web |
| Search input and icon | input `(25,22) 261×42`; 16px Roboto 400, line-height 30px; icon 21×21; pill fill `rgba(255,255,255,.68)` | input inset by 8px and displaced about +6px vertically; icon and placeholder displaced +8px horizontally | Dependent geometry and palette drift from host variables; no wrapping change | Shadow/input rules consume host variables. Text and icon sizes already matched before the fix. | Same searchbar selector fix | Resolved; computed typography, fill, radius, shadow, icon color, and geometry match within physical-pixel rounding |
| Header controls | two 44×44 buttons; 16px radii; borders `rgba(93,113,90,.16)`; active fill `rgba(95,124,97,.12)` | Same size, colors, borders, radii, and icons, but vertically centered in the 58px row | Dependent +7px vertical shift | App declarations already won; computed styles matched | Searchbar height fix | Resolved |
| Filter divider/grid | top 77; width 361; gap 8; padding-top 12; border `1px solid rgba(93,113,90,.16)` | top 90.762; width 361.905; same gap/padding/color; 0.761905px physical-pixel border | Real +13.762px vertical shift plus DPR border quantization | Every grid property except physical used width matched; later positions inherited the search row height | Searchbar height fix | Resolved: Android top 76.762; residual −0.238px is DPR/crop quantization |
| Eight labels and inputs | labels 13.12px Roboto 400, muted `#68756a`, one line; inputs 361×33, 12/36px horizontal and 8px vertical padding, 4px radius, white fill, `#d9dbd2` border | Same computed styles and 60px row pitch, translated +13/14px | Dependent layout drift; no independent typography, wrapping, spacing, color, border, or radius mismatch | After vertical realignment, non-AA differing pixels per input were `0,7,0,1,0,0,0,0` | Searchbar height fix | Resolved: Android inputs start at x 24.762/y 108.762 with 361.905×32.762 physical-grid geometry versus web x 25/y 109 with 361×33 |
| Suggestion buttons/triangle icons | 36×33 cells; `#efefef` fill; `#68756a` CSS triangles | Same styles and colors, translated with inputs | Dependent shift; residual triangle edges are DPR rasterization | Computed styles match; post-fix rect deltas stay within −0.5 to +0.857px | Searchbar height fix | Resolved |
| Header surface | `(16,12) 379×559`; panel `rgba(246,250,242,.95)`; 1px border; 22px radius; `0 12px 28px rgba(69,83,66,.07)` shadow | Same top/width/style but 13px taller | Dependent height drift | Surface content height followed the searchbar | Searchbar height fix | Resolved; post-fix Android height differs by −0.714px from physical-grid rounding |
| History list surface visible below header | x 16; margin 16px; padding 0; warm `rgba(255,252,246,.86)` panel; 22px radius; Zen border/shadow | x 0; margin 0; vertical padding 8px; white background | Independent real Ionic list cascade drift | Teapp `.zen-list-surface` lost margin/padding/background to later `ion-list` and `.list-md` rules | Raise shared selector to `ion-list.zen-list-surface`; no platform fork | Resolved: Android x 16, margin 16px, padding 0, warm panel and decorations match |
| First history row | x 17, app-owned padding/typography/colors; title remains one line | Initially x 0.762 because list inset was lost | Dependent list-surface shift | `ion-item` computed app variables already matched | List selector fix | Resolved; post-fix rect deltas are under 1px; offscreen total-list height accumulates physical-grid rounding across 24 rows |
| Tab bar and icons | y 626, height 57; white background; divider `rgba(0,0,0,.07)`; Roboto 12px labels | Same app geometry and component variables | No material drift; up to 0.667px rect shift | Computed styles match | None | Explained DPR/rasterization |

### Pixel evidence

The original normalized direct comparison changed 10,659 of 280,713 pixels (3.797%). The focused post-fix comparison changes 4,599 pixels (1.638%). Exact RGBA inequality is deliberately not used to infer layout parity because antialiasing and the DPR resample affect many equal-style pixels.

The post-fix web capture remains pixel-identical to the unchanged web reference. Selected web/Android computed properties match for display, positioning, box sizing, spacing, flex/grid behavior, typography, wrapping, colors, backgrounds, borders, radii, shadows, opacity, overflow, transforms, filters, appearance, and relevant Ionic variables. Remaining element-rect deltas are normally −0.238 to +0.905px and follow the Android physical-pixel grid.

## `history`

Status: no independent material styling or responsive-layout drift remains after the shared list-surface fix. The 4.755% changed pixels are confined to text and icons. Live measurements show equal computed declarations and identical wrapping; the repeated vertical movement is explained by the 1px Ionic row separator quantizing to 0.761905 CSS px at DPR 2.625. Phase 2 continues with `statistics`.

| Element or region | Web measurement | Android API 36 measurement | Difference and category | Evidence and winning declaration | Proposed shared fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Route, viewport, scroll, safe area | `/tabs/history`; 411×683 layout and visual viewport; DPR 1; all content scroll positions 0; safe areas 0 | Same route, 411×683 layout viewport and scroll; 411.429×683.429 visual viewport; DPR 2.625; safe areas 0 | Physical crop represents a fractional CSS viewport; no breakpoint, scroll, or safe-area drift | Both captures use the same fixture and state. The app crop is 1080×1794 physical pixels. | None | Matching runtime state |
| Fonts and Ionic mode | Roboto 400/500 loaded; document `md`; headless Chrome 151 | Same faces loaded; document and inspected components `md`; Android WebView Chrome 134 | Browser/DPR rasterizer differs; no font fallback or Ionic-mode drift | `document.fonts.status=loaded`; 400/500 checks true and 300 false in both; root family `Roboto, sans-serif` | None | Matching computed inputs |
| Collapsed header shell | header 411×82; surface `(16,12) 379×62`; panel `rgba(246,250,242,.95)`; 1px Zen border; 22px radius; `0 12px 28px rgba(69,83,66,.07)` | header 411.429×81.524; surface `(16,12) 379.429×61.524`; same fill/radius/shadow; border 0.761905px | Subpixel outer-size and border quantization only | All colors, spacing, radii, shadows, overflow, filters, and safe-area variables match. App `.zen-history-header-*` declarations win in both. | None; platform-specific sizing would fork the shared design | Explained physical grid |
| Search and header controls | row `(25,21) 361×44`, 6px gap; search 261×44; two 44×44 controls | row `(24.762,20.762) 361.905×44`; search 261.905×44; controls 44×44 | Positions −0.238 to +0.667px and used width +0.905px follow the fractional visual viewport | `ion-searchbar.zen-list-search` wins `--background`, radius, shadow, colors, min-height, and padding in both. `.zen-history-header-button` and `ion-button.zen-history-header-button` win control geometry and variables. | None | Matching styles and local geometry |
| Content and list surface | content top 82/bottom 626; list `(16,98) 379×2050`; warm panel, 16px margin, 0 padding, 22px radius, Zen border/shadow | content top 81.524/bottom 626.667; list `(16,97.524) 379.429×2044.476`; same panel, margin, padding, radius, border color, and shadow | List width differs +0.429px; total height differs −5.524px after 24 repeated physical-grid rows | `ion-list.zen-list-surface` beats later `ion-list`/`.list-md` rules in both runtimes. The Android list border is 0.761905px, the physical representation of the same 1px declaration. | Already fixed by the shared qualified list selector; no additional state-local fix | Matching material surface; cumulative DPR geometry explained |
| Ionic item host and Shadow DOM | first host/native `(17,99) 377×85.375`; inner `(33,99) 361×85.375`; input wrapper height 84.375; inner separator 1px | host/native `(16.762,98.286) 377.905×85.155`; inner `(32.762,98.286) 361.905×85.155`; wrapper height 84.393; separator 0.761905px | Each row is 0.220px shorter because the equal 1px separator declaration snaps to two physical pixels; wrapper content differs only +0.018px | `.zen-list-surface ion-item` wins transparent background, Zen color/border, 16px start and inner-end padding. Ionic parts `[part=native]`, `[part=inner]`, `[part=container]`, and `[part=detail-icon]` otherwise have equal computed values. | None: a magic fixed row height or Android-only border would be a design fork and would not generalize to other wrapping | Explained DPR layout quantization |
| Visible row cadence | row tops `99, 184.375, 269.75, 355.125, 440.5, 525.875` | `98.286, 183.44, 268.595, 353.75, 438.905, 524.06` | Top delta accumulates from −0.714px on row 1 to −1.815px on row 6; separator image centroids reach −2px | All six rows have the same 85.375/85.155 local heights and the same cause. Artificial per-row translation removes about 39% of the full diff, confirming cadence rather than a new rule. | None | Explained cumulative physical-grid effect |
| Title typography and wrapping | 16px Roboto 400, 19.2px line height, 2px block margins, `#243126`; each of the first six titles wraps to two identical lines | Exact same computed font properties, margins, color, and word breaks; line boxes are 18.667px high on the physical grid versus 19px at DPR 1 | Text range widths vary by about −2.9 to +0.5px; glyph coverage differs after resampling | Ionic `.sc-ion-label-md-s h2` is the winning size/weight/margin rule in both; the shared utility remains the color winner. Matched declaration sets are equal despite different runtime injection order. | None | Explained font rasterization after computed equality |
| Date and end-note typography | dates: 14px/20px Roboto 400, `#666`; infusion note: 14px normal, Zen muted; duration: 13.12px normal, Zen muted; no clipping | Exact same computed font properties/colors and text; widths vary by at most about 1.3px | No wrapping difference; range height/width and glyph coverage reflect DPR/browser rasterization | Ionic `.sc-ion-label-md-s p` wins date size/line-height/color. `.ion-color-medium` sets note `--ion-color-base`, while `.text-zen-muted` wins rendered note color in both. | None | Explained font rasterization |
| Detail chevrons | 20×20, opacity .25, Zen text fill; 8px start and −6px end margin | Same computed size, opacity, fill, and margins | First chevron has no non-AA differences; later chevrons differ only after accumulated row movement | Ionic item `[part=detail-icon]` and its SVG have matching attributes and computed values | None | Explained cadence plus icon rasterization |
| Tab bar, icons, labels | y 626, outer height 57; 1px top border; three equal 137px tabs; 22px icons; 12px Roboto labels | y 626.667, outer height 56.762; 0.761905px top border; tabs 137.143px; same icon and label styles | Border/width quantization and glyph rasterization; selected icon geometry differs by only +0.214/+0.381px | Ionic Material tab hosts, shadow native/inner parts, icon SVGs, and labels have matching computed visual properties. `IonContent --offset-bottom` is 57px web versus 56px Android because Ionic measures the quantized tab bar; it only changes offscreen bottom padding here. | None | Explained physical grid and rasterization |

### Pixel and declaration evidence

- Exact focused recapture hashes match the existing artifacts: the production web image is unchanged and pixel-identical to its reference; the API 36 image is repeatable.
- Repository normalization and Pixelmatch at threshold 0.1 produce 13,348 of 280,713 changed pixels (4.755%). With antialiasing included the count is 23,367 (8.324%); Pixelmatch classifies 10,019 threshold-different pixels as antialiasing.
- Every non-AA changed pixel is foreground: 122 in header glyphs/icons, 12,744 in list text/chevrons, and 482 in tab icons/labels. Broad panel fills, card fill, outer decoration, and tab fill have no threshold-exceeding pixels.
- The six visible rows preserve the same text, line counts, word breaks, colors, borders, radii, shadows, icons, spacing declarations, and Ionic variables. Remaining color-count differences are differing glyph coverage after Android physical pixels are resampled, not token drift.
- Source order differs because Ionic injects component CSS at different times, but the winning qualified app declarations and the final computed styles match. No evidence-backed shared UI change remains for this state; adding a fixed fixture-specific row height or an Android override would violate the parity strategy.

## `statistics`

Status: the user-reported header-hover and breakdown-tab regressions are resolved with shared or capture-only fixes, while the preferred current time-range selector is intentionally preserved. A paired focused capture now covers the complete visible declaration hierarchy and finds no independent computed-style drift. The state remains in progress as requested: its 2.226% direct residual is not being accepted as completed parity from this audit alone.

| Element or region | Web measurement | Android API 36 measurement | Difference and category | Evidence and winning declaration | Proposed shared fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Header back and filter controls | Both controls are 44×44 with 16px radii; resting fill is `rgba(255,255,255,.6)` | Same resting geometry and fill | The prior web reference retained `:hover` on the filter after the navigation click: its interior became `#fefefd` instead of resting `#fbfdfa`; Android has no mouse hover | Back-button crops were pixel-identical. The filter difference covered 1,753 pixels with a maximum per-channel delta of 3; `.zen-history-header-button:hover` was the only differing state | Move the Playwright pointer to `(0,0)` before every canonical capture; do not change production button CSS | Resolved capture-state artifact; improves the direct comparison without changing Android production styling |
| Time-range selector | Four 44px-tall buttons; `Total` x 20–106, `Last year` x 115–201, `Last month` x 210–295, `Last week` x 304–390; 12px radii and 1px Zen borders | Same design; normalized `Total` is 87×43px because of DPR quantization | No unwanted regression; the current selector is the explicitly preferred design | Current Tailwind utilities win after the cascade-layer correction; Android and web use the same component declarations | None; retain the current selector and the unlayered cascade | Preserved |
| Breakdown tabs | Before fix: 369×46px; 14px Roboto text with 20px line height and 13px vertical padding; two 4px black separators. After fix: 369×40px; 14px line height, same padding, no separators | Before fix: approximately 45px after normalization with the same black bars. After fix: approximately 40px with no bars | Real shared regression exposed when the Tailwind utilities began winning; not an Android-only difference | `text-sm` supplied 20px line height and explicit `border-r-4 border-black` utilities supplied the bars. The old compact result came from line-height 1 with the same 13px padding | Remove the explicit separator utilities and make `.tea-mode-tab { line-height: 1; }` | Resolved in both runtimes; the selected/inactive fills, 14px typography, 13px padding, and current card geometry remain intact |
| Runtime, fonts, and Ionic mode | 411×683 layout/visual viewport at DPR 1; headless Chrome 151; Roboto 400/500 loaded; document `md` | 411×683 layout viewport and 411.429×683.429 visual viewport at DPR 2.625; WebView Chrome 134; same Roboto faces loaded; document `md` | Fractional physical crop and browser/DPR rasterizer differ; no font fallback, breakpoint, or mode drift | Focused metadata records equal font readiness and the same computed font family/weights across all inspected nodes | None | Matching computed inputs; state remains open |
| Header shell and controls | Header 411×82; surface `(16,12) 379×62`; search 261×44; back host 48×48; filter 44×44 | Header 411.429×81.524; surface `(16,12) 379.429×61.524`; search 261.905×44; controls keep 48×48 and 44×44 | Positions differ by −0.238px after the quantized header height; no local layout or visual-property mismatch | Host, Ionic back-button native/inner parts, searchbar, and app filter button have equal inspected typography, spacing, colors, borders, radii, shadows, and final computed styles; candidate app-rule inventories are retained for debugging | None | No independent declaration drift found |
| Page shell and period group | Shell x 0, inner stack x 20; period group 371×44 with four 86.75px columns | Shell width 411.429; stack x 20; group 371.429×44 with four 86.857px columns | Group top is −0.539px; widths distribute the fractional viewport | All inspected layout, spacing, typography, colors, radii, and final computed styles match. User-agent button reset candidates differ (`none/currentcolor` versus `initial`) but the shared border utilities produce equal final styles. | None | No independent declaration drift found |
| Summary panel and metric cards | Panel 371×270.484; heading 30.4px/normal Roboto; metric grid 325×152; cards 325×70 with 1px borders | Panel 371.429×269.333; heading typography matches; grid 325.905×150.857; cards 325.905×69.429 with 0.761905px physical borders | Panel top −0.539px; repeated card borders reduce the used height by 1.143px | Computed display/grid, gaps, padding, typography, wrapping, colors, backgrounds, borders, radii, and shadows match. Text has the same content and line count. | None | Physical-grid geometry measured; no shared fix justified |
| Breakdown panel, labels, and ranking bar | Panel top 456.547; tablist 369×40; heading top 540.547; ranking panel top 577.656; first track/bar 325×8 | Panel top 454.857; tablist top 455.619; heading top 538.476; ranking panel top 575.595; first track/bar 325.905×8 | About −1.69px at the panel and −2.061px at ranking content after preceding quantized borders; local row/bar heights and final computed properties match | Paired diagnostics cover tab buttons, section typography, first two ranking rows, tracks, bars, and candidate declarations. Locally translating period/summary/breakdown regions by 1/1/2px removes 34.35% of the full non-AA diff, confirming accumulated position as a major contributor. | None yet | Declaration audit complete for visible content; parity status remains in progress |
| Pixel localization | 6,248 of 280,713 pixels differ (2.226%) | Same direct normalized pair | Header 75; period 759; summary 1,090; breakdown 3,842; tab bar 482 non-AA pixels | Diff bounds follow foreground text/icons and the displaced 8px ranking bar; broad panel/card fills do not form material mismatch regions. The report-only 12% threshold is not acceptance evidence. | Preserve current source while the residual acceptance boundary remains open | In progress; do not mark Statistics complete |

## `tutorial`

Status: the requested heading regression and the material cross-runtime modal-height defect are resolved with shared component styles. The direct residual is reduced to 4.713%, but the state remains open while its foreground rasterization boundary is reviewed; this is not a claim of completed Tutorial parity.

| Element or region | Web measurement | Android API 36 measurement | Difference and category | Evidence and winning declaration | Proposed shared fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| First-page heading before fix | 29.6px Roboto 500, 32.56px line height; three lines; first dark-glyph bbox about 65×23px | Same large three-line design; normalized glyph geometry differs only by runtime rasterization plus the existing vertical offset | Real shared size/thickness regression from the cascade change, not an Android fork | `text-[1.85rem] leading-[1.1]` began winning after Tailwind layers were removed. The older web layout used 24px/28.8px and two lines | Explicit shared `text-2xl leading-[1.2] font-normal`; retain bundled Roboto and the corrected cascade | Resolved requested design in both runtimes: 24px Roboto 400, 28.8px line height, two lines |
| Tutorial track and panel | Panel `(20,85.844) 371×511.313`; the hidden Brewing description wraps to 51.188px | Before fix, panel `(20,73.446) 371.429×536.536`; hidden description wraps to 76.786px. After fix, panel `(20,72.536) 371.429×538.357` versus current web `(20,71.719) 371×539.563` | A hidden horizontal-track page wrapped one additional 25.6px line in WebView and sized the entire modal; centered expansion shifted upper content −12px and footer +13px | Paired diagnostics cover all four page bodies and text ranges. Resolved `52vh` values differed by only 0.223px, excluding the viewport-unit hypothesis. The hidden Brewing description retained its extra line after the fix while panel height aligned within 1.206px, proving the intrinsic-height coupling | Give every tutorial body deterministic `h-[min(52dvh,440px)] overflow-y-auto` geometry | Material layout defect fixed in both runtimes; all four pages retain independent scrolling |
| Current tutorial residual | Direct current actual-to-actual comparison changes 13,230 of 280,713 pixels (4.713%), down from 32,553 (11.597%) | Panel top differs +0.817px; viewport top +0.102px; active title top +1.102px; footer top +0.327px | Remaining diff follows text, buttons, and the visible preview rather than opposed panel-edge displacement | Fail-closed paired diagnostics inspect 58 nodes per runtime with no missing or hidden required nodes. The broad panel geometry is aligned after the shared fix. | Preserve the shared fix; continue residual review after the Statistics acceptance boundary | In progress; do not mark Tutorial complete |

## Settings, session detail, and Brewing states

Status: complete fail-closed declaration collectors now cover the remaining non-modal states. No additional production CSS change is justified from the current paired evidence; used-width/height, DPR-quantized borders, and Ionic offset measurements remain recorded for residual classification. These rows advance Phase 2 but do not claim overall parity complete.

| State group | Direct current ratio | Paired diagnostic coverage | Material finding | Status |
| --- | ---: | --- | --- | --- |
| `settings-mock-scale` | 1.923% | 86 nodes per runtime: content Shadow DOM, four lists, item internals, buttons, toggles, select, typography, tabs, fonts, safe area | Final visual properties match except used 411/411.429px geometry, 1/0.761905px borders, and the already-classified 57/56px tab offset | Declaration audit recorded; no shared fix justified |
| `session-detail` | 1.848% | 53 nodes per runtime: header/back internals, content, summary, notes, metrics, Setup panel, tabs | Residual components are foreground-sized; paired styles differ only in used geometry, physical borders, tab offset, and a captured sub-frame Ionic transition value | Declaration audit recorded; residual remains classification evidence |
| Brewing idle/setup/ready/infusion/rest/ended | 0.383% / 2.209% / 0.638% / 0.640% / 0.694% / 1.262% | 29/55/51/51/51/56 nodes per runtime with exact state counts; panels, fields, controls, timers, Shadow DOM actions, text ranges, tabs, fonts, safe areas | No color, typography, spacing-token, background, radius, or shadow declaration drift. Used grid rows and 1px borders land on the Android physical grid; Ionic content offset observations remain retained rather than normalized away | State audits recorded; no Android-only styling override justified |
| Brewing live timers | Both current web states capture `0:01` | Android infusion and rest metadata record `0:01` immediately before and after the 0.73–0.78s native screenshots | Prior `0:04`/`0:05` images were capture-preparation latency, which also polluted the ended duration | Run UIAutomator system-dialog preparation before starting each timed phase, then capture without repeating it | Capture-state defect fixed and measured |
| Keyboard-open setup modal | Web panel 371×227; Android panel 371.429×226.167; body 70/69.524; actions 44/44 | Android residual inset is 24px after Ionic resize; panel, title, body, input, and actions are visible within the 375px visual viewport | The prior 332px inset double-applied the native keyboard after a resize event overwrote the resting-height baseline, collapsing the body to 0 | Preserve the pre-open/layout baseline and do not update it on the resize preceding `ionKeyboardDidShow`; compare the app panel separately from the OS keyboard | Fixed on API 36; panel-interior diff 2.413%, zero geometry/visibility failures at 1.5 CSS-px tolerance |

## Cause table

| Cause | Code change | Corrected states observed in full journey | Before → after direct changed pixels | Status |
| --- | --- | --- | --- | --- |
| Late Ionic Material searchbar declarations beat equal-specificity app declarations | `ion-searchbar.zen-list-search` | `history-filters`; the same shared header also benefits Statistics when expanded/used | `history-filters` 3.797% → 1.638% | Fixed and measured |
| Late `ion-list`/`.list-md` declarations beat equal-specificity app surface declarations | `ion-list.zen-list-surface` | `history`, `history-filters`, `settings-mock-scale`, `statistics` | `history` 5.285% → 4.755%; Settings 3.910% → 1.923%; Statistics 5.009% → 2.243% | Fixed; remaining mismatches in those other states require their own ledger rows |
| No independent collapsed-History cascade drift after the list fix | Diagnostic coverage only; no production CSS change justified | `history` | 4.755% remains, fully localized to equal-style text/icons and repeated DPR separator quantization | Explained; state ledger complete |
| Desktop pointer remained over the next route's filter control | Capture pointer reset before screenshot | `statistics` and any later click-to-route capture | Filter control returned from incidental hover to the same resting state Android already used | Fixed capture-state artifact; no production CSS change |
| Newly winning tab utilities exposed explicit separators and a 20px line height | Remove Statistics separator utilities; restore `.tea-mode-tab` line-height 1 | `statistics` in web and Android | 46px/approximately 45px strip → 40px/approximately 40px; black bars removed | Fixed and measured; preferred period selector untouched |
| Newly winning tutorial size utility made the heading too large while bundled Roboto made the weight visually heavier | Explicit 24px/1.2/400 shared heading | `tutorial` in web and Android | Three-line 29.6px/500 heading → two-line 24px/400 heading | Fixed requested causal group; full state still pending diagnosis |
| No independent visible Statistics declaration drift after the approved fixes | Focused diagnostics and `VISUAL_STOP_AFTER=statistics`; no production UI change | `statistics` | 2.226% remains; 34.35% is removed by measured local physical-grid realignment | Visible declaration audit recorded; state deliberately remains open rather than being accepted from a broad ratio |
| Hidden Tutorial page intrinsic height sized the whole horizontal track | Deterministic `52dvh` page-body height with independent overflow | `tutorial` | 11.597% → 4.713%; panel-height delta about 25px → 1.206px | Fixed and measured; residual stays open |
| Modal keyboard resize overwrote the resting app-height baseline | Retain the pre-keyboard baseline and add application-owned modal diagnostics/comparison | `brewing-setup-modal` | Broken 332px inset/body height 0 → 24px residual inset/body about 70px with all controls visible | Fixed and measured on API 36 |
| Native preparation ran after the timed phase began | Move UIAutomator preparation before each timed action and record timer pre/post screenshot | `brewing-infusion`, `brewing-rest`, `brewing-ended` | Android `0:04`/`0:05` drift → stable `0:01` during the screenshot | Fixed capture-state defect |

## Validation notes

- `visual:compare:web`: all 13 states at 0.000% against the refreshed web reference.
- Focused API 36 capture through the production journey to `history-filters`: 1.638% direct changed pixels.
- Full API 36 journey: all 13 canonical states captured after the causal group.
- Historical refresh from `da105477…`: the earlier run matched ten API 36 states exactly and exposed timer and keyboard-modal instability; those findings motivated the current fixes and are retained as history rather than current status.
- The current actual-to-actual 13-state journey passes a 5% per-state cap for every non-modal state. This is a new blocking gross-drift boundary, not a declaration that all residuals or overall parity are complete.
- Focused collapsed-History recapture: web remains 0.000% from its unchanged reference; direct API 36 comparison remains 4.755%, with the mismatch localized and classified above.
- Historical modal evidence from `da105477…`: the app crop varied between 1080×985 and 1080×1101 and the input/actions were clipped. The current reference fixes the app-owned geometry and enforces it separately from OS keyboard pixels.
- The regression-fix web and API 36 journeys both captured all 13 canonical states. The first API 36 attempt hit the known post-restore WebView reload timeout after `tutorial`; an unchanged retry completed the journey.
- Current refreshed-reference direct output reports 2.226% for Statistics and 4.713% for Tutorial. These figures remain localization aids; neither state is declared complete from the blocking gross-drift threshold.
- Paired focused Statistics recaptures record 59 inspected visible nodes per runtime, equal Roboto readiness, and no material computed-style difference after excluding used widths/heights, DPR-quantized 1px borders, and the already explained tab-bar offset. The diagnostics are retained in the capture harness for future regression evidence; Statistics remains in progress.
- Paired current captures record complete diagnostic coverage for all 13 canonical states: Tutorial 58, Settings 86, History 119/151, Statistics 59, session detail 53, modal 7, and Brewing 29/55/51/51/51/56 nodes per runtime, with zero missing or non-visible required nodes and zero count mismatches.
- The keyboard-open modal comparison enforces a 5% application-panel crop cap, 1.5 CSS-px local geometry tolerance, and visibility of panel/title/body/input/actions. Android-owned keyboard pixels remain full-device review evidence rather than web pixel-diff input.
- Web, API 36 app-content, and API 36 full-device references were clean-source recaptured from `155fda8497895554808c0bacc6b6ccf12128275d`. All 39 screenshots were reviewed; the ended summary is now `0:01`, and `baselines/unfixed` was not modified.
- Fresh target repeatability journeys match all 13 web and all 13 API 36 references at 0.000%. Direct non-modal ratios all pass the 5% blocking cap; the separate modal check passes at 2.413% with zero geometry/visibility failures.
